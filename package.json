{
  "name": "merge-sorted-arrays",
  "version": "1.0.0",
  "description": "Merge three sorted int arrays into one ascending array without using any sort function",
  "main": "dist/merge.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "start": "ts-node src/index.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
